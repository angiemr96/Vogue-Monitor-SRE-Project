apiVersion: v1
kind: Service
metadata:
  name: vogue-monitor
spec:
  selector:
    app: vogue-monitor
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31245